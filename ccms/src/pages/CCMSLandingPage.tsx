import React from "react";

const CCMSLandingPage: React.FC<any> = ({ onNavigate }) => {
  return (
    <div className="flex flex-col items-center justify-center h-screen">
      <h1>CCMS landing page</h1>
    </div>
  );
};

export default CCMSLandingPage;
